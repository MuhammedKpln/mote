{
    "name": "shared-types",
    "version": "0.0.0",
    "main": "src/index.ts",
    "types": "src/index.ts",
    "scripts": {
        "build": "tsc --build --force tsconfig.json",
        "dev": "tsc --watch --force tsconfig.json"
    },
    "dependencies": {
        "class-transformer": "^0.5.1",
        "class-validator": "^0.14.0"
    },
    "devDependencies": {
        "tsconfig": "*",
        "typescript": "^4.5.2"
    },
    "publishConfig": {
        "access": "public"
    }
}
